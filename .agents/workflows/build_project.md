---
description: Build Project
---
# Build Project Workflow

This workflow installs dependencies and builds the Next.js project.

// turbo
1. Install dependencies: `npm install`
// turbo
2. Build the project: `npm run build`
